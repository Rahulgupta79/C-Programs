#include<stdio.h>
int sum (int n);
main ()
{
int num;
printf ("Enter the number ") ;
scanf("%d",&nu) ;
printf ("Sum of digits of %d is %d\n", num, sum(n,um) );
}
int sum (int n)
c:::::
int i, sum=O, rem;
whiletn>O)" ' {' . rem=n%10;
sum+=rem;
n/=10;
return .( sum) ;
}
